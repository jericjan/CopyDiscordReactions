# CopyDiscordReactions
a BetterDiscord plugin that adds a button to the reactions window that copies all users that have reacted to a message

# to-do:
- grab user id from profile pic, do default if using default pfp
